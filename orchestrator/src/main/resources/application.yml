server:
  port: 8080

spring:
  application:
    name: soa-orquestador

servicios:
  usuarios:
    url: ${SERVICE_USUARIOS_URL:http://localhost:8081}
  clientes:
    url: ${SERVICE_CLIENTES_URL:http://localhost:8082/api}
  mesas:
    url: ${SERVICE_MESAS_URL:http://localhost:8083/api}
  qr:
    url: ${SERVICE_QR_URL:http://localhost:8084}
  ventas:
    url: ${SERVICE_VENTAS_URL:http://localhost:8085/api/v1}
  cocina:
    url: ${SERVICE_COCINA_URL:http://localhost:8086}
  inventario:
    url: ${SERVICE_INVENTARIO_URL:http://localhost:8087/api/v1}
  finanzas:
    url: ${SERVICE_FINANZAS_URL:http://localhost:8088/api/v1}

logging:
  level:
    com.orquestador: DEBUG

management:
  endpoints:
    web:
      exposure:
        include: "*"
      base-path: /actuator
    endpoint:
      health:
        show-details: always